1.What is Emmet?

Emmet can be helpful for writing JSX, which is a syntax extension for JavaScript that is used with React to describe the structure of UI components. Emmet provides shortcuts and abbreviations for generating JSX code, making it faster and easier to write complex React components.

2.Difference between a Library and Framework?

A library is a collection of pre-written code that you can use in your own projects. Examples include React, jQuery.
A framework is a more comprehensive set of tools and guidelines that dictate the structure and flow of your application.Examples include Angular, Django.

3.What is CDN? Why do we use it?

CDN stands for Content Delivery Network. It's a network of servers distributed across various locations globally. The primary purpose of a CDN is to deliver web content (such as images, scripts, stylesheets, videos, etc.) to users more efficiently and reliably.

4.Why is React known as React?

React is known as "React" because of its core concept: reacting to changes in data and rendering user interfaces dynamically.

5.What is crossorigin in script tag?

The crossorigin attribute in a <script> tag is used to specify how the browser should handle requests for the script file when loading it from a different origin (i.e., a different domain, protocol, or port).

6.What is diference between React and ReactDOM?

React provides the core functionality for building UI components and managing their state, ReactDOM provides the necessary methods and utilities for rendering those components into the DOM and interacting with them within a web browser environment. They work together to enable the creation of dynamic and interactive user interfaces in web applications.

7.What is difference between react.development.js and react.production.js files via CDN?

When you're ready to deploy your React application to a production environment, it's recommended to use the production version (react.production.js) to ensure optimal performance and minimize the size of your application bundle. During development, however, you can use the development version (react.development.js) to take advantage of its debugging features and helpful warnings to aid in the development process.

8.What is async and defer?

"async" and "defer" are attributes that can be used with the <script> tag in HTML to control how the browser loads and executes external JavaScript files. They both affect the timing of when the script is fetched and executed in relation to the parsing of the HTML document.





